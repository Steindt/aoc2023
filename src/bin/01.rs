use std::fs;

fn main() {
  let input = fs::read_to_string("../../data/examples/01b").expect("Couldn't find input file");
  for line in input.lines() {
    let mut a: &str = "";
    for char in line.chars() {
    }
  }
}